centos-6.5-x86_64-netboot
=================

Netboot CentOS 6.5 install using a kickstart file in the "http" directory provided dynamically by packer.

Steps
=====

1. Build with packer 
  ```bash
  $ packer build template.json
  ```
